# 验证记录｜2026-09-15

## 本地验证

- `uv run pytest -q`：402 项通过，包含保留的英文回归测试与新增中文 Top 5 测试。
- `uv run mypy src`：13 个源码文件类型检查通过。
- 新增检查覆盖：干净环境下的中文/北京时间/目标仓库默认值；最多五条且保留编辑排序；中文证据字段缺失时停止；恶意 Markdown 和非网页链接；事件日期与北京时间跨日；完整候选快照→决策→中文渲染；失败后不遗留旧正文。
- 工作流测试执行真实 JavaScript 日期逻辑，验证北京时间午夜边界、冻结日期与 opt-in 定时开关。
- 实际执行 29 个 RSS 源的候选采集。部分请求超时，具体结果见本地生成的 `digest-run-status.json`；该运行状态文件不提交到公开仓库。

## 本次阅读体验升级

- 中文 Markdown 与 HTML 使用相同的已验证输入，包含30秒速览、投资/产品/监管标签、来源支持的状态、跨期新增变化和一个今日行动。
- 回归检查覆盖中文字段传播、追踪日期与新增内容成对校验、来源链接转义、HTML 注入防护，以及失败时同时清理旧 Markdown/HTML。
- HTML 使用18px正文、单列自适应宽度、黑白配色和灰阶横版标识；`docs/email-preview.html` 提供公开事件版式示例。
- 浏览器的 URL 安全策略阻止了本地预览导航。本次完成了静态样式与结构检查，未宣称手机浏览器截图或真实 Gmail 客户端渲染已经验收。

## 尚未验证的外部步骤

没有执行真实模型 API 调用，没有派发 GitHub Issue 发布，也没有通过仓库发出测试邮件。GitHub Actions 能否运行、API 额度、Watch 通知邮箱与实收邮件需在启用独立仓库投递时验收。

现有直接 Gmail 定时任务与本仓库执行分开；任务处于启用状态不等同于已经验证某一天的邮件送达。
